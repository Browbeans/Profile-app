import { makeStyles } from "@material-ui/core/styles";


const mainStyle = makeStyles(
  () => ({
    mainContainer: {
      display: "flex",
      width: "100%"
    }
  }),
  {index: 1}
)

export default mainStyle;